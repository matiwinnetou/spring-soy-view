{namespace soy.example}

/**
 * Word list
 * @param words The words to display
 */
{template .clientWords}
	<ul>
		<li>{$ij.gHomePageUrl}</li>
		{foreach $word in $words}
			<li>{$word}</li>
		{/foreach}
	</ul>
{/template}